@page "/FavoriteVerses"
@using MyHebrewBible.Client.Features.FavoriteVerses.SearchToolbar

<PageHeader PageEnum="@MyHebrewBible.Client.Enums.Nav.FavoriteVerses" />

<Typeahead OnVerseSelected="ReturnedVerse" />


@* 
<TableOfContentList />
*@

<Grid OnVerseSelected="ReturnedVerse" />

@if (SelectedVerse is not null)
{
	<Card Verse="SelectedVerse" />
}

@* else { <p class="text-center">Please select a verse to view details.</p> } *@

@* ToDo: move <IntroductionContent /> to the menu *@


@code {
	public Enums.Verse? SelectedVerse;

	private void ReturnedVerse(Enums.Verse verse)
	{
		SelectedVerse = verse;
	}


}